_format_version: "3.0"
_transform: true

services:
  - name: example_service
    url: https://httpbin.konghq.com # change me to http://localhost:<port>
    routes:
      - name: example_route
        paths:
          - /mcp
